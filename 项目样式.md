# 项目样式规范

本文档记录了项目中使用的 UI 样式规范，后续开发应参照此规范保持风格一致性。

---

## 按钮样式

### 标准按钮（龙肉选择框按钮）

```typescript
// 按钮背景 + 边框 - 使用同一个 Graphics 组件
const graphics = buttonNode.addComponent(Graphics);

// 先绘制填充
graphics.fillColor = new Color(60, 60, 60, 255);  // 深灰色背景
graphics.roundRect(-width / 2, -height / 2, width, height, 8);
graphics.fill();

// 再绘制边框
graphics.strokeColor = new Color(255, 255, 255, 255);  // 白色边框
graphics.lineWidth = 4;
graphics.roundRect(-width / 2, -height / 2, width, height, 8);
graphics.stroke();

// 按钮文字
const label = labelNode.addComponent(Label);
label.fontSize = 18;  // 关闭按钮为 30
label.color = new Color(255, 255, 255, 255);  // 白色文字
label.horizontalAlign = Label.HorizontalAlign.CENTER;
label.verticalAlign = Label.VerticalAlign.CENTER;
```

**样式参数：**
| 属性 | 值 | 说明 |
|------|-----|------|
| 背景色 | `Color(60, 60, 60, 255)` | 深灰色 |
| 边框色 | `Color(255, 255, 255, 255)` | 白色 |
| 边框宽度 | `4` | 较粗边框 |
| 圆角 | `8` | 适度圆角 |
| 文字大小 | `18` | 标准按钮 / 关闭按钮 `30` |
| 文字颜色 | `Color(255, 255, 255, 255)` | 白色 |

**尺寸参考：**
- 标准按钮：`120 x 50`
- 关闭按钮（×）：`40 x 40`

---

## 弹窗样式

### 选择框面板（龙肉选择框）

```typescript
const containerNode = new Node('SelectionPanel');
const uiTransform = containerNode.addComponent(UITransform);
uiTransform.setContentSize(500, 500);

const graphics = containerNode.addComponent(Graphics);
// 背景
graphics.fillColor = new Color(0, 0, 0, 220);  // 半透明黑色
graphics.roundRect(-250, -250, 500, 500, 15);
graphics.fill();
// 边框
graphics.strokeColor = new Color(255, 215, 0, 255);  // 金色边框
graphics.lineWidth = 4;
graphics.roundRect(-250, -250, 500, 500, 15);
graphics.stroke();
```

**样式参数：**
| 属性 | 值 | 说明 |
|------|-----|------|
| 面板尺寸 | `500 x 500` | 根据内容调整 |
| 背景色 | `Color(0, 0, 0, 220)` | 半透明黑色 |
| 边框色 | `Color(255, 215, 0, 255)` | 金色 |
| 边框宽度 | `4` | 较粗边框 |
| 圆角 | `15` | 大圆角 |

---

## 文字样式

### 标题文字

```typescript
const label = node.addComponent(Label);
label.fontSize = 22;
label.color = new Color(255, 255, 255, 255);
label.horizontalAlign = Label.HorizontalAlign.CENTER;
label.verticalAlign = Label.VerticalAlign.CENTER;
```

**样式参数：**
| 属性 | 值 |
|------|-----|
| 字体大小 | `22` |
| 颜色 | `白色` |
| 对齐 | 居中 |

### 描述文字

```typescript
const label = node.addComponent(Label);
label.fontSize = 18;
label.color = new Color(255, 255, 255, 255);
label.horizontalAlign = Label.HorizontalAlign.CENTER;
label.verticalAlign = Label.VerticalAlign.CENTER;
```

### 大标题

```typescript
const label = node.addComponent(Label);
label.fontSize = 30;
label.color = new Color(255, 255, 255, 255);
```

---

## 布局规范

### 上下结构布局

```typescript
// 第一部分：贴图（单独一行，居中）
const rowNode = new Node('Row');
rowNode.setParent(containerNode);
rowNode.setPosition(0, 160, 0);  // Y 轴位置根据内容调整

// 第二部分：介绍文字（单独一行，居中）
const descNode = new Node('Description');
descNode.setParent(containerNode);
descNode.setPosition(0, 60, 0);

// 第三部分：按钮行（居中排列）
const buttonRowNode = new Node('ButtonRow');
buttonRowNode.setParent(containerNode);
buttonRowNode.setPosition(0, -60, 0);

// 按钮间距
const buttonSpacing = 20;  // 按钮之间间距
const totalWidth = buttonWidth * 3 + buttonSpacing * 2;
const startX = -totalWidth / 2;  // 从左侧开始
```

**间距参考：**
- 行间距：`80-100` 像素
- 按钮间距：`20` 像素
- 内容边距：`40` 像素

---

## 颜色规范

| 用途 | 颜色值 | 说明 |
|------|--------|------|
| 按钮背景 | `Color(60, 60, 60, 255)` | 深灰色 |
| 按钮边框 | `Color(255, 255, 255, 255)` | 白色 |
| 弹窗背景 | `Color(0, 0, 0, 220)` | 半透明黑色 |
| 弹窗边框 | `Color(255, 215, 0, 255)` | 金色 |
| 文字颜色 | `Color(255, 255, 255, 255)` | 白色 |

---

## 代码模板

### 创建按钮完整示例

```typescript
private createButton(parent: Node, name: string, text: string, pos: Vec3, width: number, height: number, onClick: () => void): Node {
    const buttonNode = new Node(name);
    buttonNode.setParent(parent);
    buttonNode.setPosition(pos);

    const btnTransform = buttonNode.addComponent(UITransform);
    btnTransform.setContentSize(width, height);

    // 按钮背景和边框
    const graphics = buttonNode.addComponent(Graphics);
    graphics.fillColor = new Color(60, 60, 60, 255);
    graphics.roundRect(-width / 2, -height / 2, width, height, 8);
    graphics.fill();
    graphics.strokeColor = new Color(255, 255, 255, 255);
    graphics.lineWidth = 4;
    graphics.roundRect(-width / 2, -height / 2, width, height, 8);
    graphics.stroke();

    // 按钮文字
    const labelNode = new Node('Label');
    labelNode.setParent(buttonNode);
    labelNode.setPosition(0, 0, 0);
    const labelTransform = labelNode.addComponent(UITransform);
    labelTransform.setContentSize(width - 10, height);
    const label = labelNode.addComponent(Label);
    label.string = text;
    label.fontSize = 18;
    label.color = new Color(255, 255, 255, 255);
    label.horizontalAlign = Label.HorizontalAlign.CENTER;
    label.verticalAlign = Label.VerticalAlign.CENTER;

    // 点击事件
    buttonNode.on(Node.EventType.TOUCH_END, onClick, this);

    return buttonNode;
}
```

---

## 更新记录

| 日期 | 更新内容 |
|------|----------|
| 2026-04-17 | 初始版本，记录龙肉选择框样式 |
